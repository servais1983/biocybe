# BioCybe — Dépendances "core"
# ============================
# Pour une installation propre, préférer :
#     pip install -e .                  (core seulement, dev local)
#     pip install -e ".[soc]"           (profil SOC complet)
#     pip install -e ".[all]"           (tout, y compris dev)
#
# Ce fichier est conservé pour rétro-compatibilité et CI rapide.
# La source de vérité des dépendances est pyproject.toml.

pyyaml>=6.0
psutil>=5.9.0
requests>=2.27.1
yara-python>=4.3.0
watchdog>=3.0.0
python-json-logger>=2.0.2
structlog>=23.0.0
cryptography>=41.0
